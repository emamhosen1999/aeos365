/**
 * SettingsSection — re-export of the shared generic section wrapper
 * (components/settings/SettingsSection.jsx). Kept at this path so the Core
 * section pages import it unchanged; the implementation is shared with Platform.
 */
export { default } from '@/components/settings/SettingsSection.jsx';
